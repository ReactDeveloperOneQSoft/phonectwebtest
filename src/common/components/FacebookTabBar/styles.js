import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tab: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'transparent'
    },
    tabs: {
        height: 60,
        flexDirection: 'row',
        alignSelf: 'stretch',
        borderTopWidth: 1,
        borderTopColor: '#CACECF'
    }
});

export default styles;
